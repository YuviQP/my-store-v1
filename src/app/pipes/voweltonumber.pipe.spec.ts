import { VoweltonumberPipe } from './voweltonumber.pipe';

describe('VoweltonumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VoweltonumberPipe();
    expect(pipe).toBeTruthy();
  });
});
